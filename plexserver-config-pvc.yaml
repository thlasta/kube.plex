apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plexserver-config-pvc
  namespace: plexserver
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-storageclass
  resources:
    requests:
      storage: 1Gi