apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plexserver-data-pvc-1
  namespace: plexserver
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 100Gi
  volumeName: plexserver-data-pv-1